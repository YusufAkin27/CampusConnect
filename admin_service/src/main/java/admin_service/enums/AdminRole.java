package admin_service.enums;

/**
 * Roles assigned to admin users in the system.
 * Each role has a predefined set of permissions.
 */
public enum AdminRole {
    SUPER_ADMIN,
    ADMIN,
    MODERATOR,
    SUPPORT,
    CONTENT_MANAGER
}
